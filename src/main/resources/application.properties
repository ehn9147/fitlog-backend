spring.datasource.driver-class-name=org.sqlite.JDBC

spring.jpa.database-platform=org.hibernate.community.dialect.SQLiteDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Use env var on Render, fall back to your Windows path locally
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:sqlite:C:/Users/Emily/IdeaProjects/workout-backend/fitlog.db}

# Use Render's PORT env, fallback to 8080 locally
server.port=${PORT:8080}
